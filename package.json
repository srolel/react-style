{
  "name": "react-stylesheets",
  "version": "1.0.7",
  "description": " ",
  "main": "index.js",
  "scripts": {
    "test": "cd build && mocha",
    "watch:test": "cd build && mocha -w",
    "watch:test:notify": "(cd build && mocha -w) | node notify.js",
    "dev": "concurrent \"babel src -w --out-dir build\" \"iron-node dev.js\"",
    "start": "babel src --out-dir build && webpack --config webpack.config.src.js && webpack --config webpack.config.min.js",
    "patch-release": "npm start && npm test && git add . --all && git commit -m \"patch\" && npm version patch && npm publish"
  },
  "author": "Sharon Rolel",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.0.12",
    "babel-plugin-transform-runtime": "^6.0.2",
    "babel-preset-es2015": "^6.0.12",
    "babel-preset-react": "^6.0.14",
    "babel-runtime": "^6.0.12",
    "chai": "^3.4.0",
    "chokidar": "^1.2.0",
    "concurrently": "^0.1.1",
    "jsdom": "^7.0.2",
    "mocha": "^2.3.3",
    "node-notifier": "^4.3.1",
    "react-addons-test-utils": "^0.14.0",
    "react-dom": "^0.14.1",
    "webpack": "^1.12.2",
    "react": "^0.14.0"
  },
  "dependencies": {
    "hyphenate-style-name": "^1.0.0",
    "reactribute": "^0.1.2",
    "tiny-emitter": "^1.0.1"
  }
}
